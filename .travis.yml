sudo: required
dist: bionic
language: cpp
compiler: gcc
cache: ccache
addons:
  postgresql: 9.5
  apt:
    packages:
      - libboost-dev
      - libboost-filesystem-dev
      - libboost-locale-dev
      - libboost-program-options-dev
      - libboost-system-dev
      - libcrypto++-dev
      - libfcgi-dev
      - libmemcached-dev
      - libpqxx-dev
      - libxml2-dev
      - libyajl-dev
script:
  - ./autogen.sh
  - ./configure --enable-yajl CC="ccache gcc" CXX="ccache g++"
  - make
  - make check
after_failure:
  - cat test-suite.log
